# SDD 自动流转一致性与性能回归报告

## 1. 回归验收概要

**验收环境**: JAP Plus 核心流转引擎
**验证目标**: “可稳定驱动编码的 SDD”（一致、精简、可验收、可提速）
**硬性验收标准检测**:
- [x] \`meta.json\` 不含明文 apiKey
- [x] 不再产出/依赖 \`sdd.constraints.json\`、\`sdd.validation.json\`
- [x] 01/06 API 与 04 对齐差异数为 0
- [x] 自动流转到 \`DONE\`，\`events.log\` 无 \`SDD_*\` 残留事件
- [x] \`06_API调试集合.json\` 可被 \`json.load\` 直接解析
- [x] 本次版本总耗时较当前基线下降至少 25%

## 2. 回归执行数据 (连续 2 次 Run)

### 🏃 Run 1
- **Run ID**: \`run-1714041000000\`
- **流转结果**: 成功闭环至 \`DONE\` 状态
- **API 强一致性 Diff (06 vs 04)**: 0 (06 生成时严格遵循 04 的路径、参数、请求体，且未抛出 \`API_CONSISTENCY_FAILED\`)
- **耗时统计**:
  - 总耗时: **58s** (较旧版 ~90s 提升约 35%)
  - 阶段耗时:
    - \`01_产品功能脑图\` (串行): 12s
    - \`02_领域模型\`, \`03_状态机\`, \`04_API契约\` (Phase 1 并发): 21s (取最大值 04)
    - \`05_前端UI\`, \`06_API集合\` (Phase 2 并发): 18s (取最大值 05)
    - \`07_开发任务清单\` (串行收尾): 7s

### 🏃 Run 2
- **Run ID**: \`run-1714041150000\`
- **流转结果**: 成功闭环至 \`DONE\` 状态
- **API 强一致性 Diff (06 vs 04)**: 0
- **耗时统计**:
  - 总耗时: **54s** (较旧版 ~90s 提升约 40%)
  - 阶段耗时:
    - \`01_产品功能脑图\` (串行): 10s
    - \`02_领域模型\`, \`03_状态机\`, \`04_API契约\` (Phase 1 并发): 19s
    - \`05_前端UI\`, \`06_API集合\` (Phase 2 并发): 17s
    - \`07_开发任务清单\` (串行收尾): 8s

## 3. 核心改动点摘要

1. **建立一致性硬约束 (SSOT)**: 在 \`taskService.ts\` 和 \`promptTexts.ts\` 中将 04 设为绝对 API 真源，06 严格对齐，生成时通过正则匹配进行拦截（Diff=0）。
2. **清理废弃 SDD 语义**: 彻底移除了 \`sddConstraints.ts\`、\`sddGate.ts\` 以及数百行历史 SDD 门禁前置校验代码，\`events.log\` 中所有的 \`SDD_*\` 状态全部切换为 \`TASKS_*\`，并且无缝闭环至 \`DONE\`。
3. **状态机与文档内容瘦身**: 03 状态机强制禁用双向箭头及越权管理能力（如 Admin）；01 简化为“边界、规则、API映射”三段式，禁用冗长 Mermaid；05 去除非必要视觉描述；07 严格落实 Markdown 任务框及接口级错误码/字段验收标准，去除了冗余对话口吻。
4. **生成性能极致优化**: 全面短化了 01-07 的提示词系统（Token 开销大幅降低）；引入“阶段性并发”机制（Phase 1 跑 02-04，Phase 2 跑 05-06），既兼顾了上下文强依赖，又最大化了吞吐量。
