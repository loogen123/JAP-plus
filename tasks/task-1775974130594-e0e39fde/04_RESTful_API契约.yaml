openapi: 3.0.3
info:
  title: J-AP Plus Ordering API
  version: 1.0.0
paths:
  /api/v1/orders:
    post:
      responses:
        '201':
          description: Created
components:
  schemas:
    Order:
      type: object
      properties:
        id: { type: string }
        tenantId: { type: string }